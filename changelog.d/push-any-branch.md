---
kind: feature
---
Push from the Changes panel (and `ralphy sync push`) now publishes whichever
branch you are on, the repo's default branch included — the remote's own branch
protection is the only gate.
