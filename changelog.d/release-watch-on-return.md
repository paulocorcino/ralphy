---
kind: fix
headline: A workbench tab that stays open now shows a new release
---
The workbench checks for a new release each time you return to its tab, not
only when the page loads. The What's new panel has a Copy button for `ralphy
update` and says to run it in a terminal outside the workbench.
