---
kind: feature
---
The Note menu is a map: every note on the plane is a row that slides the view
to its card, and a note's own `##` sections hang under it — collapsed — so a
long note is one click from the part you meant.
