---
kind: feature
---
The branch picker lists the project's worktrees under `.ralphy/worktrees/` —
name, branch and a dirty dot, with the primary tree first — and `ralphy
worktree list` prints the same listing at the shell.
