---
kind: fix
---
The Changes panel shows when a fetch, pull or push is still running: the busy
button spins and the remote bar locks until the upstream answers, so a slow
push no longer looks like a dead click.
