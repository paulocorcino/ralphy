[package]
name = "ralphy-pty"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared PTY crate for Ralphy adapters (stub; portable-pty backing lands in a later slice)."

[dependencies]
