{
  "releases": [
    {
      "version": "v0.1.0-rc.20",
      "date": "2026-09-10",
      "announce": true,
      "entries": [
        {
          "id": "console-name-opt-in",
          "kind": "breaking",
          "text": "Naming a Claude console is now something a project asks for. Turn on\n`claude.console_name` — in Settings → Claude, or with\n`ralphy config set claude.console_name true` — to keep opening consoles that\nanswer to `wb-<repo>-<hex>`; left off, Claude names each session itself, as it\ndid before the workbench started renaming them."
        },
        {
          "id": "release-check",
          "kind": "feature",
          "text": "`ralphy update --check` tells you whether the build you are running is behind\nwhat has been published, and lists every release between the two — not just the\nnewest one."
        },
        {
          "id": "release-watch-daemon",
          "kind": "feature",
          "text": "The workbench knows when a newer build exists. The daemon checks the published\nreleases every six hours, and `GET /api/release` reports the whole gap between\nwhat you are running and what is out — with what each release changed. It sends\nnothing, caches to disk, is silent when the network is absent, and switches off\nwith a marker file in the daemon store."
        },
        {
          "id": "update-command",
          "kind": "feature",
          "text": "`ralphy update` takes the newest release for you: it downloads the archive for\nyour platform, refuses it unless it matches the published checksum, replaces the\nbinary in place, and restarts the daemon so the build you are running is the one\nthat was installed."
        },
        {
          "id": "whats-new-panel",
          "kind": "feature",
          "text": "The workbench tells you when a newer build is out. A dot on the account puck,\nand a What's new panel listing every release between the one you are running and\nthe newest, with what each one changed. A fixes-only release is a quiet dot; a\nbreaking or security release stays on screen until you dismiss it. \"Stop\nchecking\" turns the whole thing off."
        },
        {
          "id": "changes-badge-reason",
          "kind": "fix",
          "text": "A project whose working tree could not be read now says so: its changes badge\nshows the reason on hover instead of the generic label."
        },
        {
          "id": "console-corner-grip",
          "kind": "fix",
          "text": "On a tablet, a console can be resized from its bottom-right corner again. With\nthe key bar shown, the row of keys used to cover the corner grip, so the one\nplace a finger reaches for did nothing; the row now ends a fingertip short of\neach corner and the grip is back where it is looked for."
        },
        {
          "id": "desk-hidden-tab-rect",
          "kind": "fix",
          "text": "Reloading the workbench from any tab other than Consoles no longer moves every\nrestored console to the top-left corner. A window restored while its tab was\nhidden measured 0×0 at 0,0, and that box was saved over its real place; the\nsaved layout now keeps the window's box until it is actually moved or resized."
        },
        {
          "id": "explorer-stale-folder-ghost",
          "kind": "fix",
          "text": "The Explorer no longer shows a folder that isn't there. Reusing a directory\nname — renaming `ideias/` away and creating a fresh `ideias/` — used to make the\nnew folder inherit the old one's contents from the browser's tree memory, so it\nlisted children it never had; expanding one answered \"not found\" and deleting it\ndid nothing at all. A directory listing now evicts what it contradicts, and a\ndelete that comes back \"not found\" re-lists the parent, so the row goes away\neither way."
        },
        {
          "id": "install-over-running",
          "kind": "fix",
          "text": "`ralphy install --force` can now replace a `ralphy` that is currently running —\nthe resident daemon being the ordinary case. It moves the old binary aside\ninstead of deleting it, which is what Windows allows, and puts it back if the\nreplacement fails."
        },
        {
          "id": "release-review-fixes",
          "kind": "fix",
          "text": "Several corrections to the release machinery, found by a review of it: the\nchangelog fold now refuses to re-fold a version whose fragments it already\nconsumed (which used to erase that release's record); `bump` refuses a version\nCargo would not accept instead of writing it into every manifest; a tag carrying\na non-ASCII character no longer aborts the version check; `ralphy update`\nrestarts the daemon onto the binary it just installed, and never ends a process\nthat only happens to share the old daemon's number."
        },
        {
          "id": "settings-panel-inert-controls",
          "kind": "fix",
          "text": "Settings no longer offers controls it cannot operate. The Schedule section and\nthe queue's \"Eligible labels\" field were never wired to anything — editing\neither came back \"config change refused\" — and are gone; use `ralphy schedule`\nfor the timer. The fallback verify command is now shown read-only, because its\nvalue names a program a later run executes and the daemon refuses to set it\nfrom a browser; set it with `ralphy config set verify.command` in the repo."
        }
      ]
    }
  ]
}
